namespace MobileId.MobileId
{
    public enum Results
    {
        OK,
        USER_CANCELLED,
        TIMEOUT,
        NOT_MID_CLIENT,
        SIGNATURE_HASH_MISMATCH,
        PHONE_ABSENT,
        DELIVERY_ERROR,
        SIM_ERROR
    }
}